# =============================================================================
# Backend Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values
# NEVER commit the .env file to version control!
# =============================================================================

# Server Configuration
PORT=3001
NODE_ENV=production

# =============================================================================
# SECURITY - REQUIRED FOR PRODUCTION
# =============================================================================
# Generate secure random strings with: openssl rand -base64 32

# Session secret (for session cookies)
SESSION_SECRET=your-secure-random-string-here

# =============================================================================
# CORS Configuration - REQUIRED FOR PRODUCTION
# =============================================================================
# Comma-separated list of allowed origins
# MUST include the Forma app origin for your region:
# - EU: https://app.autodeskforma.eu
# - US: https://app.autodeskforma.com
ALLOWED_ORIGINS=https://app.autodeskforma.eu,https://webapps.henn.com

# =============================================================================
# Application URL - REQUIRED FOR PASSWORD RESET EMAILS
# =============================================================================
# The URL where your app is deployed (used in password reset emails)
APP_URL=https://webapps.henn.com/forma-tree

# =============================================================================
# Directus Configuration (REQUIRED for user auth and project sync)
# =============================================================================
DIRECTUS_URL=https://your-directus-instance.com
DIRECTUS_STATIC_TOKEN=your-directus-static-token

# =============================================================================
# Python API URL (for tree detection service)
# =============================================================================
# Local development: http://localhost:5001
# Docker production: http://forma-trees-python:5001
PYTHON_API_URL=http://forma-trees-python:5001

# =============================================================================
# Forma API Tokens (Optional - for backend Forma API calls)
# =============================================================================
# These are used for uploading tree GLBs to Forma storage
# BEARER_TOKEN=your-forma-bearer-token
# REFRESH_TOKEN=your-forma-refresh-token

# =============================================================================
# Email Configuration (Optional - for password reset)
# =============================================================================
# Uses SMTP via Microsoft 365
# EMAIL_USER=your-email@your-company.com
# EMAIL_PASS=your-app-password
